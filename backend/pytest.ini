[pytest]
# Run from the repo root: pytest tests/ -v
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Show short tracebacks and extra summary info on failures
addopts = -ra --tb=short

# Asyncio mode for any async tests
asyncio_mode = auto